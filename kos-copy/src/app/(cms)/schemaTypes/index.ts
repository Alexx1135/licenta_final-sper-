// This file should re-export the schemas from the root schemas folder

// Adjust the path based on your actual directory structure if needed.
// '../../../../schemas' should go up four levels from src/app/(cms)/schemaTypes to the root, then into schemas.
export { schemaTypes } from '../../../../schemas';
